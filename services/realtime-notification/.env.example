# Railway → realtime service → Variables

PORT=
SYMFONY_API_ORIGIN=https://web-production-6bdab.up.railway.app
WS_INTERNAL_SECRET=use-a-long-random-secret-shared-with-symfony

# Optional FCM (paste full service account JSON as one line, or use a file path locally)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
