# Copy to .env — never commit real secrets.

PORT=5000
NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=dfd_app

JWT_SECRET=replace_with_long_random_string
JWT_EXPIRES_IN=7d

# CORS origin for Vite dev server / production URL
CLIENT_URL=http://localhost:5173
