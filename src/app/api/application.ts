/**
 * Lease booking / application API (Appdev equivalent: order.ts).
 */
export {
  fetchApplications,
  fetchApplication,
  applyToListing,
} from './mobile';

export type { Application, ApplicationListing } from './types';
