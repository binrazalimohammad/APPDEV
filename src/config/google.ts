/**
 * OAuth 2.0 Web Client ID from Google Cloud Console (used as webClientId for native sign-in).
 * Android also needs SHA-1 + package name configured for the Android OAuth client type.
 *
 * Replace before running Google Sign-In in production builds.
 */
export const GOOGLE_WEB_CLIENT_ID =
  '264534690497-j0cl3eep1p4ensqk7es8moj6npsl92o7.apps.googleusercontent.com';
